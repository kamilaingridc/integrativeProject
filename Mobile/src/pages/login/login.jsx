import { View, Text, Button } from 'react-native';
import Animated, {
    useSharedValue,
    withTiming,
    useAnimatedStyle,
    Easing,
} from 'react-native-reanimated';

export default function Login(props) {
  return (
    <View className="bg-red-30">
      <Text>Login Page</Text>
    </View>
  );
}
 